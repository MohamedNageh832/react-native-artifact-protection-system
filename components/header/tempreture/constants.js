import {COLORS} from '../../../services/colors';

const MESSAGES = {
  safe: 'Everything Good.',
  warning: 'Not ideal but ok.',
  danger: 'High tempreture. Fan on.',
};

const STATE_COLORS = {
  safe: COLORS.green,
};

export {MESSAGES, STATE_COLORS};
